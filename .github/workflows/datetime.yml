name: Datetime

on:
  push:
    branches:
      - main
  workflow_dispatch:
  
jobs:
  job:
    name: datetime
    runs-on: ubuntu-latest
    steps:
      - name: get local datetime
        run: now=`date "+%Y/%m/%d %H:%M:%S"
      - name: show local datetime
        run: echo ${now}
